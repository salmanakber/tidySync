# deploy/certs — TLS certificates (Let's Encrypt)

Place `fullchain.pem` and `privkey.pem` here after running:

```bash
sudo ./scripts/setup-tls.sh sync.tidyflowapp.com your@email.com
```

Do not commit real certificates to git.
